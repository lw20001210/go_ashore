# Database
DB_USER=exam
DB_PASSWORD=changeme
DB_NAME=shangan
DATABASE_URL=postgresql://exam:changeme@db:5432/shangan

# JWT
JWT_SECRET=your-random-secret-min-32-chars
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# DeepSeek（在 apps/api/.env 配置，不要提交到 Git）
DEEPSEEK_API_KEY=sk-你的真实密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com

# App
NODE_ENV=production

# Local dev: allow phone/LAN access to Next.js dev assets (optional, auto-detected if unset)
# DEV_ALLOWED_ORIGINS=192.168.104.83
